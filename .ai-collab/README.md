# AI 자율 협업 워크플로우 (Claude Code ↔ Codex)

Claude Code와 Codex가 **자율적으로** 협업하여 코드 작성 및 리뷰를 진행하는 시스템입니다.

## ⚡ 핵심 개념

**사용자는 개입하지 않습니다!**

1. **Claude Code** 세션: 백그라운드에서 대기 → status.json 감지 → 자동 작업 → 문서 업데이트 → 다시 대기
2. **Codex** 세션: 백그라운드에서 대기 → status.json 감지 → 자동 리뷰 → 문서 업데이트 → 다시 대기
3. 사용자는 task.md만 작성하고, 나머지는 AI들이 알아서 왔다갔다

---

## 📁 폴더 구조 (개선됨)

```
.ai-collab/
├── README.md                # 이 파일
├── CLAUDE-GUIDE.md          # Claude용 가이드
├── CODEX-GUIDE.md           # Codex용 가이드
├── QUEUE.md                 # 작업 대기열 (다중 작업 관리)
├── tasks/                   # 모든 작업 폴더
│   ├── task-001-login/
│   │   ├── task.md
│   │   ├── status.json      # 🔑 현재 턴 추적
│   │   ├── claude-work.md
│   │   └── codex-review.md
│   ├── task-002-payment/
│   └── task-003-refactor/
└── archive/                 # 완료된 작업
```

**핵심 파일: `status.json`**
- 현재 누구 차례인지 추적
- AI가 주기적으로 체크하여 자동 실행
- 사용자 개입 불필요

---

## 🚀 빠른 시작 (자율 모드)

### 1️⃣ 초기 설정 (한 번만)

**4개 터미널/세션 열기:**

```bash
# 터미널 1: Claude Code 세션 1
claude-code

# 터미널 2: Claude Code 세션 2
claude-code

# 터미널 3: Codex 세션 1
# (Codex 실행 방법)

# 터미널 4: Codex 세션 2
# (Codex 실행 방법)
```

**각 세션에 역할 부여:**

```bash
# Claude 세션 1에서:
"CLAUDE-GUIDE.md 읽고, tasks/ 폴더를 5초마다 체크하면서
내 차례(current_turn: claude)인 작업을 찾아서 자동으로 처리해줘.
완료 후 status.json을 업데이트하고 다시 대기해."

# Claude 세션 2에서: (동일)

# Codex 세션 1에서:
"CODEX-GUIDE.md 읽고, tasks/ 폴더를 5초마다 체크하면서
내 차례(current_turn: codex)인 작업을 찾아서 자동으로 리뷰해줘.
완료 후 status.json을 업데이트하고 다시 대기해."

# Codex 세션 2에서: (동일)
```

---

### 2️⃣ 작업 추가

**사용자가 할 일 (이것만!):**

```bash
# 1. 새 작업 폴더 생성
mkdir -p .ai-collab/tasks/task-001-login
cd .ai-collab/tasks/task-001-login

# 2. task.md 작성
cat > task.md << 'EOF'
# Task: 로그인 기능 구현

## 요구사항
- JWT 기반 인증
- bcrypt 비밀번호 해싱
- 로그인/로그아웃 엔드포인트

## 리뷰 레벨
CRITICAL_ONLY
EOF

# 3. status.json 생성 (Claude에게 신호 보내기)
cat > status.json << 'EOF'
{
  "task_id": "task-001-login",
  "status": "QUEUED",
  "current_turn": "claude",
  "last_actor": "user",
  "assigned_to": "claude-session-1",
  "reviewer": "codex-session-1",
  "timestamp": "$(date -Iseconds)"
}
EOF

# 4. QUEUE.md에 등록
echo "- [⏸️] task-001-login (Claude-1 할당)" >> ../../QUEUE.md
```

**끝! 이제 AI들이 알아서 처리합니다.**

---

### 3️⃣ 자동 워크플로우 (사용자 개입 없음)

```
[사용자] task.md + status.json 생성 (turn: claude)
    ↓ (Claude 세션 1이 5초마다 체크 중...)
    ↓ 감지!
[Claude-1] 코드 작성 → claude-work.md → status.json 업데이트 (turn: codex)
    ↓ (Codex 세션 1이 5초마다 체크 중...)
    ↓ 감지!
[Codex-1] 리뷰 → codex-review.md → status.json 업데이트 (turn: claude, status: REVISION_NEEDED)
    ↓ (Claude 세션 1이 계속 체크 중...)
    ↓ 감지!
[Claude-1] 수정 → claude-work.md 업데이트 → status.json (turn: codex)
    ↓
[Codex-1] 재리뷰 → status.json (turn: completed, status: APPROVED)
    ↓
[완료!] 🎉
```

**사용자는 커피 마시면서 결과만 확인! ☕**

---

## 🔄 Status.json 흐름

### 초기 상태 (사용자 생성)
```json
{
  "current_turn": "claude",
  "status": "QUEUED"
}
```

### Claude 작업 완료 후
```json
{
  "current_turn": "codex",
  "status": "WAITING_FOR_CODEX_REVIEW",
  "last_actor": "claude-session-1",
  "timestamp": "2025-12-19T17:45:00Z"
}
```

### Codex 리뷰 완료 후
```json
{
  "current_turn": "claude",
  "status": "REVISION_NEEDED",
  "last_actor": "codex-session-1",
  "critical_issues": 2
}
```

### 최종 승인
```json
{
  "current_turn": "completed",
  "status": "APPROVED",
  "completed_at": "2025-12-19T18:00:00Z"
}
```

---

## 📊 다중 작업 관리 (QUEUE.md)

### 시나리오: 3개 작업 동시 진행

**QUEUE.md:**
```markdown
## 진행 중
- [🟢] task-001-login (Claude-1, Codex 리뷰 대기)
- [🟢] task-002-payment (Claude-2, 구현 중)

## 대기 중
- [⏸️] task-003-refactor (높음)
- [⏸️] task-004-bugfix (긴급)
```

**자동 할당:**
- Claude-1 → task-001 처리
- Claude-2 → task-002 처리
- Codex-1 → task-001 리뷰 대기
- Codex-2 → task-002 리뷰 대기

**task-001 완료 시:**
- Claude-1 자동으로 task-003 시작

**긴급 task-004 추가 시:**
- 현재 작업 완료 후 큐 맨 앞으로

---

## ⚠️ 충돌 방지

### ✅ 병렬 처리 가능
```
Claude-1: src/auth/login.js
Claude-2: src/payment/checkout.js
→ 다른 파일이므로 안전!
```

### ❌ 병렬 처리 불가
```
Claude-1: src/utils.js 수정
Claude-2: src/utils.js 리팩토링
→ 같은 파일! 순차 처리 필요!
```

**QUEUE.md에 충돌 여부 표시:**
```markdown
- [⏸️] task-005-refactor-utils (⚠️ task-003 완료 필요)
```

---

## 🔢 멀티 세션 전략

### 질문 1: Claude 2개 + Codex 2개 문제 없나?

**답: 문제 없습니다! 단,**

1. **각 세션에 명확한 역할 부여**
   ```
   Claude-1: task-001, task-003 담당
   Claude-2: task-002, task-004 담당
   Codex-1: 홀수 task 리뷰
   Codex-2: 짝수 task 리뷰
   ```

2. **status.json에 세션 명시**
   ```json
   {
     "assigned_to": "claude-session-1",
     "reviewer": "codex-session-1"
   }
   ```

3. **충돌 체크**
   - 같은 파일 수정하는 작업은 순차 처리
   - QUEUE.md에서 의존성 명시

### 질문 2: 수많은 기능 작업 관리?

**답: QUEUE.md + 우선순위**

```markdown
## 대기열 (20개 작업)

### 긴급 (즉시 처리)
- task-010-security-patch
- task-015-critical-bug

### 높음 (이번 주)
- task-001-login
- task-002-payment
- task-005-api-v2

### 보통 (다음 주)
- task-011-refactor
...

### 낮음 (여유 있을 때)
- task-020-docs
```

**전략:**
1. 긴급: 즉시 세션 할당
2. 높음: 순차 처리
3. 보통/낮음: 여유 세션에 할당

**세션 활용:**
```
4개 세션으로 동시에 2개 작업 진행
(Claude 2개 + Codex 2개 = 2 작업/사이클)

20개 작업 = 10 사이클
각 사이클 평균 30분 = 5시간 완료
```

---

## 🛠️ 실전 예시

### 예시: 5개 기능 구현

```bash
# 1. 작업 5개 생성
for i in {001..005}; do
  mkdir -p tasks/task-$i-feature$i
  cat > tasks/task-$i-feature$i/task.md << EOF
# Feature $i 구현
...
EOF
  cat > tasks/task-$i-feature$i/status.json << EOF
{
  "task_id": "task-$i",
  "current_turn": "claude",
  "assigned_to": "claude-session-$((i % 2 + 1))"
}
EOF
done

# 2. QUEUE.md 업데이트
# (생략)

# 3. 4개 세션 시작 (이미 실행 중)

# 4. 사용자는 커피 마시며 대기 ☕

# 5. 30분 후 확인
grep "APPROVED" tasks/*/status.json
# task-001: APPROVED ✅
# task-002: REVISION_NEEDED (진행 중)
# task-003: APPROVED ✅
# task-004: WAITING_FOR_CODEX_REVIEW
# task-005: IN_PROGRESS
```

---

## 🎚️ 리뷰 레벨 (동일)

### CRITICAL_ONLY (권장)
- 보안, 버그, 로직만
- 빠르고 저렴

### STANDARD
- 위 + 에러 핸들링, 엣지 케이스

### THOROUGH
- 모든 것 (비쌈)

---

## 💡 베스트 프랙티스

### 1. 작업 크기
- ✅ 작은 작업 (1-2시간): 빠른 피드백
- ❌ 큰 작업 (1일+): 분할 필요

### 2. 우선순위 활용
```markdown
긴급: 버그, 보안 (즉시)
높음: 핵심 기능 (이번 주)
보통: 일반 기능 (다음 주)
낮음: 개선 (여유 있을 때)
```

### 3. 세션 관리
- Claude 2개: 서로 다른 모듈
- Codex 2개: 리뷰 대기 큐 소화

### 4. 충돌 방지
- 같은 파일 수정 = 순차 처리
- 의존성 있음 = 순차 처리
- 독립적 = 병렬 처리 OK

---

## 🆘 문제 해결

### Q: AI가 멈춰있어요
A: status.json 확인. `current_turn`이 해당 AI인지 체크.

### Q: 무한 루프에 빠졌어요
A: status.json에서 `iteration` 카운터 확인. 5회 초과 시 수동 개입.

### Q: 충돌이 발생했어요
A: `.git/conflicts.log` 확인. 순차 처리로 변경.

### Q: Codex 비용이 너무 많이 나와요
A: 모든 task.md에서 `CRITICAL_ONLY` 명시.

---

## 📞 명령어 (초기 설정용)

### Claude 세션 시작
```bash
"CLAUDE-GUIDE.md 읽고 자율 모드로 시작해.
tasks/ 폴더를 5초마다 체크하고
current_turn이 claude인 작업을 자동 처리해줘."
```

### Codex 세션 시작
```bash
"CODEX-GUIDE.md 읽고 자율 모드로 시작해.
tasks/ 폴더를 5초마다 체크하고
current_turn이 codex인 작업을 자동 리뷰해줘."
```

### 수동 개입 (긴급 시)
```bash
# Claude에게
"task-001의 status.json을 completed로 바꿔줘"

# Codex에게
"task-002를 강제로 APPROVED 처리해줘"
```

---

## 🎯 다음 단계

1. ✅ [QUEUE.md](QUEUE.md) 확인 - 다중 작업 관리 방법
2. ✅ [CLAUDE-GUIDE.md](CLAUDE-GUIDE.md) - Claude 상세 가이드
3. ✅ [CODEX-GUIDE.md](CODEX-GUIDE.md) - Codex 상세 가이드
4. ✅ tasks/TASK-TEMPLATE/ - 작업 템플릿
5. 🚀 첫 작업 생성해보기!

---

## 🌟 핵심 정리

1. **사용자**: task.md + status.json 생성만
2. **AI들**: 자율적으로 감지 → 작업 → 업데이트 → 대기
3. **다중 작업**: QUEUE.md로 관리
4. **멀티 세션**: 충돌만 주의하면 문제 없음
5. **결과**: 커피 한 잔 사이에 여러 작업 완료! ☕

**행운을 빕니다! 🚀**
